## TODO

---

## QA

### 一、项目深挖

#### *你说 FalcoEval 是“大模型评估系统”，它和 lm-evaluation-harness、OpenCompass、HELM 这类已有框架相比，核心差异在哪里？*
结论先给：
FalcoEval 的定位不是“通用 benchmark 框架”，而是一个**面向工程闭环的轻量评估系统**。它和 lm-evaluation-harness、OpenCompass、HELM 的核心差异，本质上在于三点：**设计目标、系统边界、以及工程取舍**。
第一层：设计目标差异（research benchmark vs engineering tool）
这些主流框架本质是“研究导向 benchmark 平台”。
例如：
lm-eval：强调 reproducibility + 标准 benchmark（MMLU、HellaSwag）
OpenCompass：强调大规模模型横向对比 + leaderboard
HELM：强调多维评估（公平性、鲁棒性、风险）
而 FalcoEval 的目标不同，它是：
 **为模型迭代提供快速、可控、可定制的评估闭环**
换句话说，它更像一个**工程内评测系统**，而不是一个“论文 benchmark 平台”。
第二层：系统边界差异（闭环 vs 松散工具）
FalcoEval 是一个**端到端闭环系统**：
配置 → 推理 → 答案抽取 → 指标计算 → 性能分析 → 结果落盘
这一点和 lm-eval 这种“只负责 evaluation，不管生成细节”的框架不同。
具体差异：
FalcoEval：
内置推理（SGLang / Transformers）
内置 prompt 构造 + reference 构造
内置答案抽取 pipeline
内置性能 profiling（p50/p95/tokens/s）
lm-eval / OpenCompass：
更偏“评测调度器”
推理通常依赖外部接口（OpenAI API / vLLM / 自建服务）
很少关心 latency / throughput
所以一句话总结：
 FalcoEval = **评测系统（evaluation system）**
 lm-eval / OpenCompass = **评测框架（evaluation framework）**
第三层：Prompt & Answer 处理机制差异（工程化 vs 静态）
这是一个面试容易加分的点。
FalcoEval 里：
`Promptor`：根据任务动态构造 prompt
`Referencer`：动态生成标准答案
`answer_extraction`：统一抽取模型输出（解决格式不一致问题）
而 lm-eval / OpenCompass：
多数任务是**静态 prompt + 静态 reference**
很少有统一的“答案抽取层”
这带来一个工程上的核心价值：
 FalcoEval 能适配“非标准输出模型”，而 benchmark 框架通常假设模型是规范输出的
第四层：性能评估维度（这是明显差异点）
FalcoEval 明确把**性能指标**纳入一等公民：
TTFT / latency（p50 / p95）
tokens/s
throughput
而：
lm-eval：几乎不关心性能
OpenCompass：性能支持有限
HELM：更偏质量维度
所以它更接近真实业务需求：
 不只是“模型好不好”，而是“能不能上线”
FalcoEval：
优点：
结构简单（predict / eval 解耦）
新任务接入成本低（改 Promptor + Evaluator）
可控性强（本地模型、本地数据）
缺点（可以主动说，面试加分）：
benchmark 覆盖不如 OpenCompass
评测标准不完全统一（需要自己维护一致性）
缺少社区验证
最后给你一版“面试标准总结句”：
FalcoEval 和 lm-eval / OpenCompass / HELM 的核心差异在于：
它不是一个通用 benchmark 平台，而是一个面向模型迭代的工程化评测系统。
它提供从推理、prompt 构造、答案抽取到指标计算和性能分析的完整闭环，更强调可控性、可扩展性和性能评估，而不是大规模标准 benchmark 对比。


#### 你为什么要自己实现一套评测框架，而不是直接基于 OpenCompass 二次开发？

#### FalcoEval 的整体架构是什么？请你从数据加载、prompt 构造、模型推理、答案解析、指标计算完整讲一遍。
FalcoEval 整体上是一个端到端的大模型评测流水线，我把它拆成了几个相对独立的模块：数据加载、prompt 构造、模型推理、答案解析、reference 构造、指标计算和结果汇总。

首先是**数据加载层**。系统会根据用户选择的数据集，读取统一格式的数据文件。不同 benchmark 原始格式差异很大，比如选择题有 question、choices、answer，问答任务可能有 context、question、answers，数学题一般是 question 和最终答案。所以我先把不同数据集预处理成统一的 jsonl 格式，并在加载时做字段校验，比如检查 question、choices、answer 是否存在，过滤明显无效的样本。这样后续评测流程不需要关心每个原始数据集的细节。

第二步是**任务路由和 prompt 构造**。每个数据集会映射到一个任务类型，比如多选题、判断题、抽取式问答、生成式问答、数学题等。系统根据任务类型选择对应的 prompt 模板。比如选择题会把选项格式化成 A、B、C、D，并要求模型只输出选项；问答任务会拼接 context 和 question；数学任务会强调只输出最终答案。这样做的目的是让不同任务的输入形式尽量标准化，同时减少模型输出冗余内容。

第三步是**模型推理层**。FalcoEval 把推理后端封装成统一接口，上层只需要传入一批 prompts，不需要关心底层是 SGLang 还是 Transformers。默认使用 SGLang 做高吞吐批量推理，适合大规模 benchmark；同时保留 Transformers 后端作为兼容和降级方案。推理时会按 batch 生成结果，并记录原始输出、输入 token 数、输出 token 数、推理耗时等信息。对于连续评测多个数据集的场景，系统会复用已经加载的模型，避免反复冷启动。

第四步是**答案解析层**。模型原始输出通常并不完全规整，比如选择题可能输出 “The answer is B”，数学题可能先写推理过程再给最终数字。所以系统会根据任务类型做后处理：选择题抽取 A-F 选项，判断题归一化成 yes/no，数学题优先抽取 boxed answer 或最后一个数字，问答和生成任务则保留文本并做基本清洗。这个步骤是为了把模型自由生成的文本转成可以和标准答案比较的预测结果。

第五步是**reference 构造层**。系统会从数据集中构造标准答案。对于选择题，如果原始答案是选项文本，就转换成对应的 A/B/C/D；如果是多参考答案的问答任务，就保留多个 reference，后续评测时取最高匹配分；判断题和数学题则保留标准答案字符串。这样可以统一 prediction 和 reference 的比较方式。

第六步是**指标计算层**。系统会根据任务类型和用户选择的指标计算结果。选择题、判断题和数学题主要用 Accuracy；抽取式问答常用 EM 和 F1；生成式任务可以用 BLEU、ROUGE；同时还会计算一些效率指标，比如平均 latency、p50、p95、tokens/s、平均输出长度、冷启动时间和 warmup 时间。这样评测结果不只反映模型答得对不对，也能反映模型推理成本和服务效率。

最后是**结果汇总和展示层**。系统会把每个数据集的预测结果、标准答案、原始输出、质量指标和效率指标统一保存，并通过 Gradio UI 展示。用户可以在界面上注册模型、选择多个数据集、配置指标，然后一键运行完整评测。
所以总结来说，FalcoEval 的架构核心是：用统一数据格式和任务类型映射屏蔽 benchmark 差异，用模块化流水线串起 prompt、推理、解析和评估，用 SGLang 提升大规模推理效率，最终输出兼顾效果和性能的评测结果。
#### 你简历里写“任务路由、数据构造、模型推理、输出解析、指标计算模块解耦”，具体是怎么解耦的？代码里对应哪些类或文件？

#### 新增一个数据集时，需要改哪些地方？如果要做到完全配置化，你会怎么设计？

#### 目前数据集 schema 不统一，你是如何做统一加载和校验的？

#### 为什么选择 jsonl 作为统一数据格式？相比 parquet、json、HF Dataset 有什么优缺点？

#### 多选题、短问答、生成式问答、数学题的 prompt 模板有什么区别？为什么这样设计？

#### 你如何保证不同模型评测时 prompt 是公平的？
我会从三个层面保证 prompt 的公平性。
第一是**同一数据集使用完全相同的 prompt 模板**。FalcoEval 不是针对某个模型单独写 prompt，而是根据任务类型统一生成模板。比如所有选择题模型都看到同样的 question、choices 和“只输出选项”的约束；所有问答任务都按同样方式拼接 context 和 question。这样可以避免因为给某个模型写了更适配的提示词而造成结果偏差。
第二是**输入内容和选项顺序保持一致**。评测时不会根据模型特点调整样本内容，也不会对不同模型使用不同的 few-shot 示例或额外解释。对于选择题，A/B/C/D 的选项顺序直接来自同一份标准化数据；对于问答任务，context 和 question 也来自同一个预处理版本。也就是说，不同模型面对的是同一批样本、同一种表达方式和同一套约束。
第三是**解码参数和后处理规则统一**。公平性不只体现在 prompt，也包括生成配置和答案解析。评测时我会统一 temperature、max_new_tokens、batch size 等关键参数，通常用 temperature=0 保证确定性；模型输出后也用同一套任务级答案抽取规则，比如选择题统一抽取 A-F，数学题统一抽取最终答案。这样可以减少因为采样策略或解析方式不同造成的误差。
当然，prompt 公平也有一个 trade-off：不同模型的 chat template 可能不同，比如 Qwen、Llama、ChatGLM 对 system/user 格式的偏好不一样。我的做法是尽量在任务内容层保持一致，同时如果使用模型官方 chat template，也要对所有模型遵循同样原则：只做必要格式适配，不额外加入模型特定提示。这样既保证输入语义一致，也避免因为格式不兼容导致某些模型吃亏。
总结来说，我会保证：**同数据、同模板、同选项顺序、同解码参数、同答案解析规则**。如果必须做模型格式适配，也只做 chat template 层面的最小适配，不改变任务信息本身。
#### 模型输出经常不是严格格式，比如选择题输出 “I think the answer is B”，你怎么做答案抽取？
我会把答案抽取做成一个**任务类型相关的后处理模块**，先保留模型的 raw output，然后根据任务类型把自由文本转成可评测的标准 prediction。
以选择题为例，目标是把各种输出都归一化成 `A/B/C/D` 这种选项标签。模型可能输出：
```text
B
The answer is B.
I think the answer is B because...
Option (B)
答案是：B
```
对于这类情况，我会优先用规则抽取明显的选项模式，比如匹配 `answer is B`、`option B`、`答案：B`、括号里的 `(B)` 等。如果匹配成功，就返回对应的大写字母。
如果没有匹配到完整模式，我会做一个兜底策略：取输出清洗后的首个有效选项字符，比如第一个出现的 `A-F`。但这个兜底要谨慎，因为模型解释里可能同时出现多个选项，比如 “A is wrong, B is correct”，这时简单取第一个字母会出错。所以更稳的做法是先匹配带有“answer / option / choice / 答案”等关键词的模式，再考虑兜底。
对于其他任务，我也会做不同的解析规则。判断题会把 `yes/true` 归一化成 `yes`，把 `no/false` 归一化成 `no`；数学题会优先抽取 `\boxed{}` 里的答案，如果没有，再取最后出现的数字或分数；抽取式问答和生成式任务通常保留文本，只做去空格等基础清洗。
同时我会保留两份结果：一份是 raw output，用于排查模型实际回答；另一份是 parsed prediction，用于指标计算。这样如果评测结果异常，可以回看原始输出判断是模型真的答错了，还是答案抽取规则出了问题。

总结来说，我的策略是：**先按任务设计高精度规则抽取，再做保守兜底，并始终保留原始输出用于可追溯分析**。

#### 当前答案抽取规则有哪些边界问题？比如数学题输出多个数字怎么办？

#### 选择题 reference 是选项字母还是选项文本？为什么？

#### 你实现的 EM/F1 和 SQuAD 官方评测有什么差异？

#### ROUGE、BLEU 适合评估开放式生成任务吗？它们的缺陷是什么？
ROUGE 和 BLEU 可以作为开放式生成任务的**参考指标**，但我不会把它们当成唯一判断标准。
它们的优势是计算简单、稳定、成本低，而且不依赖额外模型。比如在摘要、翻译、短文本生成这类有标准参考答案的任务里，ROUGE 可以衡量生成结果和 reference 的 n-gram 或最长公共子序列重合度，BLEU 可以衡量生成文本和参考文本之间的 n-gram precision。所以它们适合做大规模自动评测里的基础指标。
但它们的缺陷也很明显。
第一，**过度依赖字面重合**。开放式生成经常存在多个正确表达，比如“乔布斯创立了苹果公司”和“苹果公司的创始人之一是 Steve Jobs”语义接近，但如果字面重合少，ROUGE/BLEU 分数可能很低。
第二，**不能可靠判断语义正确性**。模型可能复述了 reference 里的很多词，但逻辑关系错了、实体错了，ROUGE/BLEU 仍然可能不低。反过来，模型语义正确但表述不同，也可能被低估。
第三，**对长短和风格敏感**。BLEU 偏向精确匹配，短答案容易不稳定；ROUGE 更偏召回，模型如果输出很长，可能覆盖更多 reference 词，从而得到较高分，但这不一定代表回答质量更好。
第四，**无法评估事实性、推理过程和安全性**。开放式生成任务里，用户关心的不只是文字相似，还包括是否真实、是否完整、是否有幻觉、是否遵循指令、是否符合领域规范，这些 ROUGE/BLEU 都很难覆盖。
所以在 FalcoEval 里，我会把 ROUGE/BLEU 作为生成任务的低成本 baseline 指标，用来做粗粒度横向比较；但如果是严肃评估开放式回答，我会补充语义类指标，比如 embedding similarity、BERTScore，或者引入人工评测/LLM-as-a-judge，并配合事实一致性、引用准确性、答案完整性等维度来评估。

#### TokenEfficiency 是怎么定义的？这个指标是否合理？

#### 简历里写支持 acc/f1/bleu 等指标，为什么不同任务要选择不同指标？

#### 对长文本 QA、法律、医学任务，简单 EM/F1 是否足够？如果不够你会加什么指标？

#### 如果模型答案语义正确但字面不同，EM/F1 会低估，你怎么解决？

#### 是否考虑过 LLM-as-a-judge？它的可靠性问题怎么控制？
考虑过。LLM-as-a-judge 很适合补充传统指标，尤其是开放式问答、摘要、长文本生成这类任务。因为 ROUGE、BLEU、EM/F1 更偏字面匹配，而 LLM judge 可以从语义正确性、完整性、逻辑性、事实一致性、指令遵循等维度给出更接近人工判断的评分。
但我不会直接把一个 LLM 的打分当成最终结论，因为它有明显可靠性问题，主要包括：
第一是**模型偏置**。Judge 可能偏好某种表达风格、某个模型家族，甚至对更长、更流畅的答案给更高分。
第二是**位置偏置**。如果让 judge 在 A/B 两个回答里选择，放在前面的回答可能更容易被选中，所以 pairwise 评测要交换顺序做两次。
第三是**尺度不稳定**。同样是 1 到 5 分，不同样本、不同 prompt 下分数尺度可能漂移。
第四是**幻觉和误判**。Judge 本身也可能不知道正确答案，尤其是法律、医学、数学、事实性问题，可能把错误答案判成正确。
第五是**和被评模型同源导致偏袒**。比如用某个模型家族做 judge，可能更偏好同家族模型的输出风格。
控制可靠性的话，我会从几个方面做：
首先，**设计结构化评分 rubric**。不要只问“哪个回答更好”，而是明确维度，比如正确性、完整性、事实一致性、简洁性，每个维度给定义和评分标准，并要求输出结构化 JSON，降低随意性。
第二，**固定 judge prompt 和解码参数**。temperature 设为 0，保证结果尽量可复现；所有模型输出都用同一套评审 prompt，不给某个模型特殊说明。
第三，**做位置随机化和顺序交换**。pairwise 比较时，把 A/B 顺序交换再评一次，只有两次结论一致才认为结果可靠；不一致则标为 tie 或进入人工复核。
第四，**多 judge 或多轮投票**。可以用多个能力较强、来源不同的 judge 模型投票，减少单个模型偏置。如果成本有限，也可以对高风险样本或边界样本才使用多 judge。
第五，**引入 reference 和证据约束**。对于有标准答案的任务，把 question、reference answer、模型回答一起给 judge，让它重点判断回答是否覆盖 reference；对于 RAG 或事实性任务，把检索证据也提供给 judge，并要求只基于证据评分。
第六，**用人工标注集校准 judge**。抽样一部分数据做人工评审，计算 judge 和人工的一致性，比如 Cohen’s Kappa、Spearman 相关系数，观察它在哪些任务上可靠，哪些任务上不可靠。
第七，**保留传统指标作为 sanity check**。LLM-as-a-judge 不替代 EM/F1/Accuracy，而是作为补充。如果 judge 分数和传统指标大幅背离，需要抽样检查原因。
所以我的观点是：LLM-as-a-judge 可以显著提升开放式任务的评估能力，但必须通过 **rubric 结构化、参数固定、位置消偏、多 judge 投票、reference/证据约束、人工校准** 来控制可靠性。否则它本身也会成为一个不透明且有偏的评测模型。

#### 如何防止评测集泄漏？如果模型训练中见过 benchmark，该怎么判断？
防止评测集泄漏可以分成两件事：**评测前尽量降低泄漏概率，评测后判断结果是否异常可信**。
评测前，我会先控制数据来源。尽量使用有明确 license 和版本记录的 benchmark，记录数据集版本、下载时间、样本数量和 hash，保证后续复现时用的是同一份数据。对于内部业务评测，最好构建私有 holdout set，不公开题目，且和训练、微调、RLHF 数据严格隔离。
第二，要做**训练数据去重和污染检测**。如果我能访问训练语料，会把 benchmark 的 question、answer、context 和训练语料做 exact match 和 near-duplicate 检测。exact match 可以用 hash 或 n-gram；near-duplicate 可以用 MinHash、SimHash、embedding similarity，检查是否存在高度相似样本。尤其是一些经典 benchmark，比如 MMLU、GSM8K、HumanEval，很容易出现在网页、教程或开源数据集中，需要重点排查。
第三，要避免在评测流程中泄漏答案。比如 prompt 里不能无意拼接 answer 字段；数据预处理时要检查 context 是否包含直接答案；few-shot 示例不能和 test set 样本重复；日志和缓存里也不要被后续训练再次采入。
如果怀疑模型训练中见过 benchmark，我会从几个角度判断。
第一是看**异常高分**。如果一个模型在公开 benchmark 上分数明显高于同规模模型，但在私有集、改写集或新构造集上掉得很厉害，这通常是污染信号。
第二是做**扰动测试**。对原题做语义等价改写、变量替换、选项打乱、数字替换、实体替换。如果模型只会答原题，轻微变形后性能大幅下降，说明可能记住了题目而不是掌握能力。
第三是做**canary 或 membership inference**。如果可以设计私有 canary 样本，并观察模型是否能异常准确复现某些罕见文本，就能判断训练集中是否包含类似内容。对于公开 benchmark，也可以检查模型是否能补全题目原文、答案解析甚至数据集特有格式。
第四是比较**公开集和新鲜集表现**。比如用最近发布、训练截止时间之后的数据，或者自己构造同分布的新题。如果模型在老 benchmark 上很高，在新 benchmark 上明显回落，就要警惕 benchmark memorization。
第五是看**错误模式**。真正掌握能力的模型错误通常有一定逻辑；记忆型模型可能在原题上答得很准，但遇到选项顺序变化、否定问法、数字替换时出现不合理错误。
所以总结来说，我会用三类手段控制：**数据隔离和版本追踪防止泄漏，exact/near duplicate 检测排查污染，扰动集/私有集/新鲜集验证模型是否只是记忆 benchmark**。如果发现污染风险，就不能只报告原 benchmark 分数，而要同时报告去污染集或变体集上的结果。

### 二、性能与推理后端

#### 你为什么选择 SGLang 作为高吞吐推理后端？
选择 SGLang 的核心原因是：FalcoEval 的瓶颈不是单条样本推理，而是大批量 benchmark 的离线生成吞吐。SGLang 更适合把大量样本组织成高并发、高吞吐的推理任务。
我的回答可以这样说：
FalcoEval 的评测流程里，模型生成预测是最耗时的阶段。如果直接用 Transformers 逐条生成，工程上最简单，但 GPU 利用率低，batch 调度能力弱，跑几千到几万条样本时耗时很长。所以我把 SGLang 作为高吞吐后端，把 Transformers 保留为兼容性和 fallback 后端。
SGLang 的优势主要有三点。
第一，它适合批量离线推理。评测任务通常是大量 prompt 独立请求，不需要复杂交互状态，天然适合 continuous batching。SGLang 可以把多个请求动态合批，提高 GPU 利用率，降低整体评测时间。
第二，它对长 prompt、多样本生成更友好。评测集中不同样本的输入长度不一致，如果手写 PyTorch/Transformers batch，padding 浪费会比较明显；SGLang 这类推理后端在调度和 KV cache 管理上更成熟，吞吐更稳定。
第三，它工程集成成本相对可控。FalcoEval 只需要把 prompt 列表交给推理后端，然后拿回文本输出，不需要深度侵入模型结构。也就是说，SGLang 被设计成一个可替换的 backend，而不是和评测逻辑耦合在一起。
我没有只保留 SGLang，而是同时保留 Transformers，是因为 SGLang 并不覆盖所有情况。比如有些自定义模型、特殊 tokenizer、特殊 chat template 或本地调试场景，Transformers 更稳。因此系统设计上是：
SGLang 用于高吞吐正式评测，Transformers 用于兼容性、调试和兜底。
从软工角度看，这其实是一个 backend abstraction 的设计：评测主流程不关心底层是 SGLang 还是 Transformers，只关心输入 prompt、输出 completion 和性能统计。这样后续如果要接 vLLM、OpenAI API 或内部推理服务，也只需要新增一个 backend adapter，而不需要改评测主流程。

#### SGLang 和 vLLM 的核心区别是什么？

#### SGLang 的批量推理性能提升来自哪里？
SGLang 批量推理的性能提升主要来自几个方面。
第一是**连续批处理，也就是 continuous batching**。传统推理如果按固定 batch 跑，必须等一个 batch 里的所有请求都生成完才能处理下一批，请求长短不一时会有很多等待。SGLang 会把不同请求的生成过程动态调度起来，某些请求结束后，新的请求可以立刻补进来，提高 GPU 利用率。
第二是**KV Cache 复用和高效管理**。大模型自回归生成时，每生成一个 token 都会复用前面 token 的 key/value。如果每次都重新算前缀，成本很高。SGLang 会管理 KV Cache，减少重复计算，尤其是在 prompt 有共享前缀、多轮对话或结构化程序调用场景下收益明显。
第三是**前缀缓存，也就是 prefix caching**。很多评测任务的 prompt 模板是固定的，比如选择题都包含相同的 instruction，只是 question 和 choices 不同。如果系统能缓存相同前缀的计算结果，就可以减少 prefilling 阶段的开销。
第四是**prefill 和 decode 阶段的调度优化**。LLM 推理一般分两段：prefill 处理输入 prompt，decode 逐 token 生成。Prefill 更偏计算密集，decode 更偏访存和调度。SGLang 会对这两个阶段做更细粒度的调度，让 GPU 不至于在 decode 阶段因为 batch 太小而利用率下降。
第五是**面向结构化生成的运行时优化**。SGLang 本身支持把复杂的 LLM 调用流程写成程序，比如多次调用、分支、约束解码等。它的 runtime 会尽量把这些调用合并和调度，而不是每一步都从 Python 层单独发起请求，从而减少调度开销。
放到 FalcoEval 里，收益主要来自两个点：一是评测场景天然有大量独立样本，可以组成较大的 batch；二是短答案任务输出 token 少，如果用普通逐条 Transformers 推理，GPU 利用率很低，而 SGLang 的连续批处理和 KV Cache 管理能显著提高吞吐。
所以我会总结为：**SGLang 的性能提升不是单纯因为 batch size 大，而是来自动态 batching、KV cache 管理、prefix caching，以及 prefill/decode 调度优化共同提高 GPU 利用率。**

#### 简历写“短答案类任务万级样本 5min 推理，单张 A100”，这个结果是怎么测的？

#### 这个 5min 包含模型加载时间吗？包含 warmup 吗？

#### batch size 怎么设置？过大或过小分别有什么问题？

#### 你怎么统计 p50/p95 latency？是单样本 latency 还是 batch 平摊？
这里要先区分清楚：**p50/p95 latency 统计的是请求级、单样本维度的 latency，不应该用 batch 总耗时简单平摊**。
在 FalcoEval 里，latency 的统计目标是衡量“一个样本从提交到拿到输出的端到端耗时”，所以更合理的定义是：
```text
latency_i = end_time_i - start_time_i
```
然后对所有样本的 `latency_i` 排序，取中位数作为 p50，取 95 分位作为 p95。
这和 batch 平摊耗时不同。batch 平摊通常是：
```text
avg_latency = batch_total_time / batch_size
```
它更接近 throughput 指标，而不是用户感知 latency。比如一个 batch 里有 32 个样本，总共 3.2 秒，平摊是 0.1 秒，但实际上每个样本都可能等了接近 3.2 秒才拿到结果。用平摊值算 p50/p95 会严重低估真实延迟。
所以我会把指标分成两类：
```text
latency：按单样本请求统计，用于 p50 / p95
throughput：按整体任务统计，例如 samples/s、tokens/s
```
在 SGLang 这种 continuous batching 后端里，这个区分更重要。因为请求会被动态调度，不同样本的排队时间、prefill 时间、decode 时间都可能不同。理想情况下，每条样本要记录自己的 `start_time` 和 `end_time`，得到 request-level latency；而整体吞吐则用：
```text
total_tokens / total_wall_time
```
如果当前实现里是按 batch 记录耗时，那我会在面试里主动说明这是一个工程近似：
当前版本如果是 batch-level timing，那么 p50/p95 更准确地说是 batch latency 的分位数，不能严格代表单样本 latency。后续应该把 timing 下沉到每条 request，记录 request_id、start_time、first_token_time、end_time，从而区分 queue latency、TTFT 和 total latency。
这句话很重要，因为它体现你不是只会报指标，而是知道指标的语义边界。
更完整的答案可以总结成：
p50/p95 应该统计单样本 request-level latency，而不是 batch 平摊 latency。batch 平摊主要用于估算吞吐，不能代表真实延迟。工程上我会为每个样本记录 start/end time，并进一步记录 first token time，这样可以同时得到 total latency、TTFT、p50/p95 以及 tokens/s。如果现有版本采用 batch-level timing，我会明确标注它是 batch latency，并在后续改造成 request-level profiling。

#### tokens/s 是怎么计算的？输入 token 和输出 token 是否都算？

#### 不同任务 max_new_tokens 不同，如何避免生成长度影响性能比较？

#### SGLang 后端失败时 Transformers 降级方案怎么工作？

#### Transformers 后端为什么吞吐通常低于 SGLang？

#### 如果要支持多卡 tensor parallel，你的代码如何传递 tp_size？

#### tensor parallel 和 data parallel 在评测场景下分别适合什么情况？

#### 多数据集连续评测时，你如何避免重复加载模型？

#### 模型 session 复用可能带来什么状态污染问题？

#### 如果多个用户同时在 Gradio UI 发起评测，会有什么并发问题？
可以坦诚说这是当前版本还没有完整解决的点，然后补上你的判断和改进方案。比如可以这样回答：
当前 FalcoEval 的 Gradio UI 更偏向单用户实验环境，我还没有把它设计成严格的多用户并发服务。如果多个用户同时发起评测，主要会有几个问题。
第一是**全局模型状态冲突**。系统里为了避免重复加载模型，会维护一个当前活跃的 predictor。如果用户 A 正在用模型 A 评测，用户 B 同时切换到模型 B，可能触发模型卸载和重新加载，影响用户 A 正在进行的任务。
第二是**GPU 资源竞争**。多个评测任务同时跑，会争抢显存和计算资源，轻则吞吐下降、latency 变高，重则 OOM。尤其是大模型加载本身就占用大量显存，多用户同时加载不同模型基本不可控。
第三是**输出文件冲突**。如果不同用户评测同一个数据集，默认预测结果路径可能相同，可能出现互相覆盖结果的问题。
第四是**进度和 UI 状态混乱**。Gradio 的 progress、结果展示和后端任务如果没有 session 级隔离，多个请求同时执行时，用户看到的进度和结果可能不对应自己的任务。
如果要把它改成多用户可用，我会做几件事：
首先，取消简单的全局 predictor，改成**任务级或会话级资源管理**。每个评测请求生成唯一 job id，结果、日志和中间文件都按 job id 隔离。
其次，引入**任务队列**，比如 Redis Queue、Celery，或者更简单地用单机队列。GPU 评测任务不直接并发跑，而是排队执行，或者按 GPU 数量限制并发度。
第三，做**模型实例池和锁机制**。如果多个用户请求同一个模型，可以复用同一个已加载实例；如果请求不同模型，则根据显存情况决定排队、拒绝或卸载。模型切换必须加锁，不能在其他任务使用时直接 shutdown。
第四，输出文件使用**唯一路径**，比如 `outputs/{job_id}/{dataset}_pred_ref.json`，避免覆盖。
第五，前端改成**异步任务查询模式**。用户提交任务后拿到 job id，前端轮询或 SSE 获取状态，包括 pending、running、failed、completed，而不是把长任务绑定在一个同步请求里。
所以我会明确说：当前版本主要面向单用户本地评测，多用户并发还没有工程化完成。我的下一步方案是用 **job id + 任务队列 + GPU 并发控制 + 模型实例锁 + 输出隔离**，把它从实验工具升级成可多人使用的评测服务。

#### 当前系统是否支持断点续评？如果中途失败怎么办？

#### 如果一个数据集有 10 万样本，你会如何做流式处理和结果落盘？

#### 如何设计评测日志，便于复现实验？
可以坦诚说目前只是保存了基础结果，还没有完整实验日志系统，然后讲你会怎么设计。口头可以这样答：
这个点我当前版本还没有完整实现。现在 FalcoEval 主要保存预测结果、reference、raw output 和部分运行指标，但如果要做到严格复现实验，我会设计一套更完整的 evaluation manifest，也就是每次评测都生成一份结构化日志。
我会把日志分成几类信息。
第一类是**实验身份信息**，比如 run id、创建时间、运行机器、代码 commit hash、运行入口、操作者，以及输出目录。这样后续能准确定位是哪一次实验。
第二类是**模型信息**，包括模型名称、模型路径或 HuggingFace id、模型版本、权重 hash、tokenizer 版本、推理后端，比如 SGLang 或 Transformers，以及 tensor parallel、dtype、量化方式等配置。因为同一个模型名背后可能有不同 checkpoint，不记录清楚就很难复现。
第三类是**数据集信息**，包括数据集名称、版本、样本数量、文件路径、文件 hash、预处理脚本版本、过滤规则、实际参与评测的样本 id 列表。这个很重要，因为 benchmark 经常有不同版本，或者预处理后样本数会变。
第四类是**prompt 和解码配置**，包括 prompt 模板版本、system prompt、few-shot 示例、chat template 使用方式、temperature、top_p、top_k、max_new_tokens、stop words、batch size、random seed。很多评测差异其实不是模型能力差异，而是 prompt 或 decoding 参数差异。
第五类是**运行环境信息**，比如 Python 版本、核心依赖版本、CUDA 版本、GPU 型号、显存、SGLang/Transformers 版本。这样可以解释一些性能指标差异。
第六类是**结果和异常信息**，包括每条样本的 raw output、parsed prediction、reference、是否解析失败、是否推理失败、耗时、输入输出 token 数；聚合层面保存 Accuracy、EM、F1、ROUGE、BLEU、latency p50/p95、tokens/s 等指标。
我会让每次评测生成一个独立目录，例如：
```text
outputs/runs/{run_id}/
  manifest.json
  predictions.jsonl
  metrics.json
  errors.jsonl
  stdout.log
```
其中 `manifest.json` 记录复现实验所需的配置，`predictions.jsonl` 记录逐样本输出，`metrics.json` 记录聚合指标，`errors.jsonl` 单独记录失败样本。
如果再进一步，我会支持用同一个 `manifest.json` 一键重跑实验，系统读取里面的模型、数据集、prompt、解码参数和指标配置，尽可能还原当时的评测设置。
所以我会总结为：当前版本还没有完整实验日志系统，但我认为可复现日志至少要记录 **代码版本、模型版本、数据版本、prompt 版本、解码参数、运行环境、逐样本输出和聚合指标**。这样别人拿到日志后，才有可能判断一个分数到底是怎么来的。

#### 如果模型输出为空或推理报错，你的系统如何处理？

### 三、常见落地追问

#### 如果让你为一个业务模型搭建离线评测体系，你会怎么设计？
我会把离线评测体系分成四层：**数据集层、评测执行层、指标层和分析闭环层**。

首先是**数据集层**。我不会只用公开 benchmark，而是会根据业务场景构建一套分层评测集。第一部分是通用能力集，比如基础问答、推理、数学、指令遵循，用来判断模型底座能力；第二部分是业务核心集，比如真实用户 query、典型业务流程、领域知识问答；第三部分是风险集，比如安全合规、幻觉、拒答、敏感内容；第四部分是回归集，把历史线上 bad case、投诉 case、版本回退 case 固化下来。每条样本都要有任务类型、难度、业务场景、标准答案或评分 rubric 等元信息。

第二是**评测执行层**。我会设计成统一流水线：加载数据、构造 prompt、调用模型、保存原始输出、做答案解析、计算指标。执行时要固定模型版本、prompt 版本、解码参数、数据版本和代码 commit，保证可复现。对于大规模评测，会支持批量推理、任务队列、失败重试、断点续跑和日志落盘。

第三是**指标层**。不同业务不能只看一个总分。客观题可以用 Accuracy、EM、F1；开放式回答可以用 LLM-as-a-judge 加人工抽检；RAG 场景要拆成检索召回率、证据相关性、答案 groundedness、最终答案正确率；对话场景要看多轮一致性、上下文保持、工具调用成功率；同时还要统计 latency、tokens/s、成本、超时率、失败率等工程指标。最终会设计一个加权 scorecard，但同时保留各维度分数，避免总分掩盖问题。

第四是**分析闭环层**。每次评测不仅输出分数，还要输出错误分析，比如按任务类型、难度、业务线、错误类型切片，定位模型在哪些场景退化。对于版本迭代，要做 baseline 对比，只允许在核心指标不下降、风险指标不过线、成本和延迟可接受的情况下进入灰度。线上出现 bad case 后，要回流到离线回归集，形成持续更新的评测闭环。

如果是生产环境，我还会特别注意三点：第一，评测集要防泄漏，业务 holdout set 和训练数据严格隔离；第二，评测日志要完整记录模型、数据、prompt、参数和环境，保证结果可复现；第三，要建立人工抽检机制，用小规模高质量人工标注校准自动指标，尤其是 LLM-as-a-judge。

所以整体设计可以总结为：**用分层数据集覆盖真实业务，用统一流水线保证可复现执行，用多维指标衡量效果、风险和成本，用 bad case 回流形成持续迭代闭环**。

#### 如果让你把这个项目变成生产可用系统，你优先改哪三点？
如果要把 FalcoEval 从实验型工具变成生产可用系统，我会优先改三点：**可复现性、任务调度与资源隔离、数据和指标体系标准化**。

第一是**可复现性**。当前版本已经能保存预测、reference、raw output 和一些运行指标，但还不够完整。生产评测必须知道一个分数是怎么来的，所以我会引入完整的 run manifest，记录模型版本、权重路径或 hash、tokenizer 版本、数据集版本和 hash、prompt 模板版本、解码参数、代码 commit、依赖版本、GPU 环境、运行时间等信息。每次评测生成唯一 run id，并保存逐样本输出、解析结果、错误日志和聚合指标。这样后续如果分数异常，可以准确复盘，也可以复跑验证。

第二是**任务调度与资源隔离**。当前 Gradio 更偏单用户本地使用，多用户并发时会有全局 predictor 冲突、GPU 资源竞争、结果文件覆盖等问题。生产化后我会改成异步任务模式：用户提交评测任务后生成 job id，后端通过队列调度任务，按 GPU 资源限制并发。模型加载和卸载要有锁或模型实例池，同一个模型可以复用，不同模型根据显存情况排队或拒绝。输出目录也要按 job id 隔离，避免互相覆盖。

第三是**数据和指标体系标准化**。当前项目接入了不少 benchmark，但不同数据集 schema、任务类型和指标适配还不够严格。生产化后我会定义统一的数据协议，比如每条样本必须包含 id、task_type、input、reference、metadata，并通过 schema 校验保证数据质量。指标侧也要做成插件化，每个指标明确适用任务、输入格式和异常处理方式，避免比如把 span precision 用到 yes/no 任务这种错配。同时要补充单元测试和端到端 mock 测试，保证新增数据集或指标不会破坏已有评测。

所以我会优先做这三件事：**用 run manifest 解决复现问题，用队列和资源管理解决并发问题，用 schema 和指标插件化解决扩展可靠性问题**。这三点完成后，FalcoEval 才能从“能跑通评测”升级到“多人、多模型、多数据集下稳定可信地跑评测”。

#### 离线 benchmark 分数和线上用户体验不一致怎么办？
#### 如何设计一套模型上线前的准入标准？
#### 如何评估模型幻觉？
我会先区分幻觉类型，再针对不同场景设计评估方法。因为“幻觉”不是一个单一指标，它通常包括**事实性错误、无依据生成、引用错误、与上下文矛盾、过度编造**等问题。

如果是**闭卷问答**，也就是模型只依赖自身参数回答，我会重点评估 factual correctness。做法是构建有标准答案的事实问答集，比如人物、时间、地点、定义、数值类问题，然后判断模型回答是否和 reference 一致。简单问题可以用 EM/F1，开放答案可以用人工或 LLM-as-a-judge，但 judge prompt 里要明确只看事实正确性，不看表达流畅度。

如果是**RAG 或长上下文问答**，我会更关注 groundedness，也就是回答是否被给定证据支持。这里不能只看答案对不对，还要看答案有没有依据。我会把评估拆成几项：检索证据是否包含答案、回答中的关键 claim 是否能在证据中找到支持、是否引入了证据外信息、引用位置是否正确。可以用 claim-level 检查，把回答拆成原子事实，再逐条判断是否被 context 支持。

如果是**摘要任务**，我会评估 consistency，也就是生成摘要是否和原文一致。方法是抽取摘要中的实体、关系、数字、时间等关键事实，和原文进行对齐；也可以用 NLI 模型或 LLM judge 判断“原文是否蕴含摘要中的陈述”。

如果是**工具/Agent 场景**，幻觉还包括工具结果伪造，比如模型没有查到却编造结果、没有调用工具却声称调用了。我会检查工具调用日志和最终回答是否一致，要求所有外部事实都能追溯到工具 observation。

指标上，我会设计几个维度：

- **事实正确率**：回答中的核心事实是否正确。
- **证据支持率 / groundedness**：回答 claims 中有多少能被上下文或检索证据支持。
- **无依据信息比例**：回答里有多少内容来自模型编造。
- **引用准确率**：引用的文档、段落、链接是否真的支持对应陈述。
- **拒答准确率**：当证据不足或问题不可回答时，模型是否能正确拒答，而不是强行编造。

评估流程上，我会采用自动评测和人工抽检结合。大规模样本用规则、NLI 模型或 LLM-as-a-judge 做初筛；高风险领域，比如医疗、法律、金融，必须加入人工标注。并且要做 adversarial 测试，比如给模型提供互相矛盾的证据、无答案问题、过期事实、带诱导性前提的问题，观察它是否会顺着错误前提编造。

所以总结来说，我评估幻觉不会只问“答案像不像 reference”，而是会看：**事实是否正确、是否被证据支持、有没有证据外编造、引用是否准确、证据不足时是否能拒答**。对于 RAG 和 Agent 场景，还要做到 claim-level 可追溯。

#### 如何评估模型安全性？
#### 如何评估长文本能力？
#### 如何评估 RAG 系统，是评估检索还是生成？
#### 如果线上模型变慢了，你会从哪些角度排查？
#### 如果模型效果下降但代码没变，可能是什么原因？
#### 如何做模型 A/B test？
#### 如何控制评测成本？
#### 如何保证评测结果可复现？
#### 如何判断一个 benchmark 是否已经不可靠？

#### 如果业务数据没有标注，怎么做自动评测？
如果业务数据没有标注，我会先把问题拆开：没有人工标准答案，并不代表完全不能评测。可以用**弱标注、自动构造、模型评审和人工抽检校准**结合起来做。

第一种方法是**从业务日志里构造弱标签**。比如搜索、推荐、客服、问答场景里，用户点击、停留时长、追问率、转人工率、投诉率、点赞点踩、是否解决问题，都可以作为弱监督信号。它们不一定等价于“答案正确”，但可以反映用户是否满意。离线评测时可以把这些信号转成粗粒度标签，比如高满意、低满意、未解决。

第二种方法是**规则或系统状态生成标签**。有些业务任务虽然没有人工标注，但可以通过确定性规则判断。比如工具调用是否成功、SQL 是否能执行、接口返回是否符合 schema、订单状态是否查对、分类结果是否命中业务规则。这类任务可以用程序自动验收。

第三种方法是**利用已有文档或数据库生成 QA**。如果业务有知识库、FAQ、商品库、政策文档，可以用信息抽取或 LLM 自动生成问题和标准答案，再经过去重、质量过滤，形成一批弱标注评测集。对于 RAG 场景，还可以从文档 chunk 自动生成问题，reference 就来自对应证据片段。

第四种方法是**LLM-as-a-judge**。对开放式回答，可以让强模型基于用户问题、业务文档、模型回答进行评分，重点看正确性、完整性、是否基于证据、是否违反业务规则。但这个不能直接无脑相信，需要固定 rubric、temperature=0、多 judge 或抽样人工校准。

第五种方法是**对比评测**。如果没有绝对标准答案，可以做 pairwise comparison：同一批业务 query，让旧模型和新模型分别回答，再用 judge 或人工判断哪个更好。这样比直接打绝对分更稳定，也更符合版本迭代场景。

第六种方法是**小规模人工金标集校准**。完全不做人工是不现实的。我会抽取高频、高价值、高风险 query，人工标注一小批 gold set，用来校准自动评测指标，观察 LLM judge、弱标签和人工判断的一致性。之后大规模样本用自动评测，小规模样本持续人工抽检。

所以整体方案是：先用业务日志、规则校验、文档生成和 LLM judge 建立自动评测信号，再用小规模人工标注做校准。这样即使没有现成标注，也能逐步形成一套可用的离线评测体系。关键是不能把弱标签当真值，要明确它们的噪声，并用人工金标集持续验证。

#### 你觉得大模型算法工程师最核心的能力是什么？
